{% extends 'base.html.twig' %}

{% block title %} StoreFreeze {% endblock %}

{% block body_id %}app{% endblock %}

{% block header %}

{% endblock %}

{% block main %}
    {% include 'baseOpen.html.php' %}

    <div class="mdl-tabs__tab-bar backgroundCreate">
        <a class="mdl-navigation__link mdl-tabs__tab" href="createStore">Ajouter Produits</a>
        <a class="mdl-navigation__link mdl-tabs__tab" href="readStore">Voir Produits</a>
    </div>

    <section class ="paragraphe subtitle backgroundCreate">
        <div class="black">
            <h3>Créer produit</h3>
        </div>
        <div> 
            {{ form_start(form, {
                attr: {
                    novalidate : "novalidate",
                    class: "row"
                }
            }) }}
            {# {{ form_start(form2, {
            attr: {
                novalidate: "novalidate",
                class: "row"
            }
            }) }} #}
            <div class="border col-12 col-lg6">
                <br>
                {{ form_row(form.name) }}
                <br>
                {{ form_row(form.Category) }}
                <br>
                {{ form_row(form.quantityGr) }}
                <br>
                {{ form_row(form.quantityUnit) }}
                <br>
                {{ form_row(form.dlc) }}
                <br>
                {{ form_row(form.Freezer) }}
                <br>
                <button class="material-icons mdl-button--myColorValidate mdl-js-button" type="submit">done_outline</button>
                <br>
                {{ form_end(form) }}
                {# {{ form_end(form2) }} #}
                <br>
            </div>
            <br>
        </div>                    
        {% include 'baseClose.html.php' %}
    </section>
{% endblock %}
